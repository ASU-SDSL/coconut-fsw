{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildnflash",
            "command": "./debug/buildnflashjtag.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Build and flash the Coconut Flight Software"
        }
    ]
}