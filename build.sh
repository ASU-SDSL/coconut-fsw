# To debug linker/compilation issues (this is a CMake feature not anything we added)
# export VERBOSE=1

# Release build
# ./deploy.sh -b

# Debug build
./deploy.sh -b -g

# Simulator
# ./deploy.sh -b -s
